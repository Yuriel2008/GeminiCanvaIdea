# codebase | 1 files | 2026-07-11 17:16:05

// index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LingoFlow Evolution - Audit v2.1</title>
    <style>
        :root {
            --bg: #020617; 
            --violet: #8b5cf6; 
            --cyan: #06b6d4; 
            --glass: rgba(255, 255, 255, 0.05);
            --border: rgba(255, 255, 255, 0.1);
            --text: #e2e8f0;
            --success: #22c55e;
            --error: #ef4444;
        }
        body, html {
            margin: 0; padding: 0;
            height: 100%; width: 100%;
            background: var(--bg);
            color: var(--text);
            font-family: 'Segoe UI', sans-serif;
            overflow: hidden;
        }
        /* Scanline Effect from your 404 page */
        body::after {
            content: "";
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.1) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.02), rgba(0, 255, 0, 0.01), rgba(0, 0, 255, 0.02));
            background-size: 100% 4px, 3px 100%;
            pointer-events: none;
            z-index: 10;
        }
        /* Horizontal Slider */
        .slider {
            display: flex;
            width: 700%; 
            height: 100vh;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
        }
        section {
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 60px;
            box-sizing: border-box;
            text-align: center;
        }
        /* Ghost Navigation Circles */
        .nav-btn {
            position: fixed;
            top: 0; bottom: 0;
            width: 10%;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            cursor: pointer;
        }
        .nav-btn.left { left: 0; }
        .nav-btn.right { right: 0; }
        .circle {
            width: 50px; height: 50px;
            border-radius: 50%;
            border: 1px solid var(--border);
            background: rgba(255,255,255,0.03);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0; 
            transition: 0.4s;
            color: white;
            backdrop-filter: blur(5px);
        }
        .nav-btn:hover .circle {
            opacity: 1;
            border-color: var(--cyan);
            box-shadow: 0 0 20px var(--cyan);
            transform: scale(1.1);
        }
        /* Comparison Cards */
        .grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            width: 100%;
            max-width: 1000px;
            margin-top: 30px;
        }
        .card {
            background: var(--glass);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 25px;
            text-align: left;
            transition: 0.3s;
        }
        .card h3 { margin-top: 0; color: var(--cyan); font-size: 1.1rem; text-transform: uppercase; }
        .card p { color: #94a3b8; font-size: 0.95rem; line-height: 1.5; }
        h1 { font-size: 3.5rem; margin: 0; text-shadow: 0 0 20px var(--violet); }
        h2 { color: var(--violet); text-transform: uppercase; letter-spacing: 2px; }
        .highlight-v2 { border-left: 4px solid var(--violet); }
        .logic-card { border-top: 4px solid var(--cyan); }
    </style>
</head>
<body>
    <div class="nav-btn left" onclick="moveSlide(-1)"><div class="circle">❮</div></div>
    <div class="nav-btn right" onclick="moveSlide(1)"><div class="circle">❯</div></div>
    <div class="slider" id="slider">
        <!-- Slide 1: Intro -->
        <section>
            <h2 style="color: var(--cyan)">LingoFlow v2.1</h2>
            <h1>EVOLUTION AUDIT</h1>
            <p>Comparing the Static Foundations to the Modern Neural Network.</p>
        </section>
        <!-- Slide 2: Architecture -->
        <section>
            <h2>The Tech Stack</h2>
            <div class="grid">
                <div class="card">
                    <h3>Version 1.0</h3>
                    <p>Static HTML/JS environment. Data was transient and localized to the browser session. No security layer.</p>
                </div>
                <div class="card highlight-v2">
                    <h3>Version 2.0</h3>
                    <p>Full-Stack PHP 8.x + MariaDB. Implements persistent sessions, PDO security, and server-side CRUD operations.</p>
                </div>
            </div>
        </section>
        <!-- Slide 3: UI Design -->
        <section>
            <h2>Visual Identity</h2>
            <div class="grid">
                <div class="card">
                    <h3>V1 Aesthetic</h3>
                    <p>Standard educational "Clean Blue" theme. Focus on functional matching blocks.</p>
                </div>
                <div class="card highlight-v2" style="background: rgba(139, 92, 246, 0.05);">
                    <h3>V2 Cyberpunk</h3>
                    <p>Deep Navy (#020617) with Neon Violet HUDs. Glassmorphism and pulsing "Active" states on the Roadmap.</p>
                </div>
            </div>
        </section>
        <!-- Slide 4: Strengths - Data Integrity -->
        <section>
            <h2 style="color: var(--success)">Strength: Persistence</h2>
            <div class="grid">
                <div class="card" style="grid-column: span 2;">
                    <h3>The Progress Engine</h3>
                    <p>Unlike v1, v2 uses a <code>user_progress</code> table. This allows for complex "Skill Trees" where lessons are locked/active/completed based on real database triggers. XP and Streaks are now meaningful rewards.</p>
                </div>
            </div>
        </section>
        <!-- Slide 5: Strength - Advanced Randomization -->
        <section>
            <h2 style="color: var(--cyan)">Strength: Randomization Logic</h2>
            <div class="grid">
                <div class="card logic-card">
                    <h3>Fisher-Yates Implementation</h3>
                    <p>The new <code>shuffleArray()</code> ensures that multiple-choice options are mathematically random, preventing "positional memorization."</p>
                </div>
                <div class="card logic-card">
                    <h3>The Pattern Breaker</h3>
                    <p>Implemented logic that checks if the correct answer is in the same slot as the previous question. If so, it re-shuffles to maintain psychological challenge.</p>
                </div>
            </div>
        </section>
        <!-- Slide 6: Strength - Admin Infrastructure -->
        <section>
            <h2 style="color: var(--violet)">Strength: System Oversight</h2>
            <div class="grid">
                <div class="card" style="grid-column: span 2;">
                    <h3>Admin Command Center</h3>
                    <p>The inclusion of <code>admin/dashboard.php</code> allows for management of "Operatives" (Users) and "Inventory" (Lessons). This transforms LingoFlow from a game into a manageable platform.</p>
                </div>
            </div>
        </section>
        <!-- Slide 7: Weakness Audit -->
        <section>
            <h2 style="color: var(--error)">Current Weaknesses</h2>
            <div class="grid">
                <div class="card" style="border-left: 4px solid var(--error);">
                    <h3>Tactile Loss</h3>
                    <p>v2 is more robust, but v1's "Pair Matching" was more tactile for vocabulary. Transitioning some v2 lessons back to "Matching" would balance the gameplay.</p>
                </div>
                <div class="card" style="border-left: 4px solid var(--error);">
                    <h3>CDN Reliability</h3>
                    <p>The Admin suite relies on external CDNs (Tailwind/FontAwesome). For true Cyberpunk "offline" reliability, these should be hosted locally.</p>
                </div>
            </div>
        </section>
    </div>
    <script>
        let current = 0;
        const total = 7;
        const slider = document.getElementById('slider');
        function moveSlide(dir) {
            current += dir;
            if (current < 0) current = 0;
            if (current >= total) current = total - 1;
            slider.style.transform = `translateX(-${current * 100}vw)`;
        }
        window.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowRight') moveSlide(1);
            if (e.key === 'ArrowLeft') moveSlide(-1);
        });
    </script>
</body>
</html>

